import { ExternalIds } from '../interfaces/external-ids.interface';

export const EMPTY_EXTERNAL_IDS: ExternalIds = {
  isrc: '',
};

export const MOCK_EXTERNAL_IDS: ExternalIds = {
  isrc: 'US1234567890',
};
